[project]
name = "jev-browser-agent"
version = "0.1.0"
description = "NL-instruction browser automation POC: planner LLM decomposes steps, Jev grounds elements, Playwright executes."
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.63,<2",
    "httpx>=0.28,<1",
]

[tool.uv]
package = false
